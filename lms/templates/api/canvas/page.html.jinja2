<!doctype html>
<html lang=en>
  <head>
    <meta charset=utf-8>
    <link rel="canonical" href="{{canonical_url}}" />
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
    {{body|safe}}
  </body>
</html>
