{
  "extends": [
    "hypothesis"
  ],
  "rules": {
    "prefer-arrow-callback": "error",
    "prefer-const": ["error", { "destructuring": "all" }],

    // Handled by Prettier.
    "comma-dangle": "off"
  }
}
